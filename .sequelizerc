const pathResolver = require("path").resolve;
// const resolve = path.resolve;

module.exports = {
  config: pathResolver("src", "config", "database.mjs"),
  "models-path": pathResolver("src", "models"),
  "seeders-path": pathResolver("src", "database", "seeders"),
  "migrations-path": pathResolver("src", "database", "migrations"),
};
